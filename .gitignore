# Xcode / build
build/
DerivedData/
*.xcodeproj/
*.xcworkspace/
!*.xcworkspace/contents.xcworkspacedata
xcuserdata/
*.xcuserstate
.swiftpm/
.build/

# macOS
.DS_Store

# Coverage artifacts
*.profraw
*.profdata

# Agent/editor local state — never published
.claude/
.cursor/
.vscode/
.idea/

# Vendored dnsmasq build artifacts (source tarball is fetched, never committed)
Resources/ThirdParty/dnsmasq/source/
Resources/ThirdParty/dnsmasq/build/
Resources/ThirdParty/dnsmasq/dist/

# Release output
Release/
*.dmg
*.zip

# Secrets — must never be committed
*.p12
*.mobileprovision
*.cer
notary-credentials*

# The implementation specification this project was written against. Kept locally; the
# repository publishes the result, not the brief.
MacNetLab v0.1：macOS 本地网络服务工具实施工单.md
